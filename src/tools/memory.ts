/**
 * Memory read/update tools.
 *
 * Thin wrappers around MemoryManager, exposed as pi ToolDefinitions
 * so the agent can read and update persistent memory.
 */

// TODO: implement as pi ToolDefinition
